{
  "name": "git-worktree",
  "displayName": "Git Worktree",
  "description": "Manage git worktree's with ease",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.72.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:git-worktree.helloWorld"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "git-worktree.helloWorld",
        "title": "Hello World"
      }
    ],
    "configuration": [
      {
        "id": "wt-general-settings",
        "title": "General Settings",
        "properties": {
          "gitWorktree.window.openNewWindow": {
            "type": "boolean",
            "default": true,
            "description": "Open new vscode window when you switch or create a worktree"
          },
          "gitWorktree.push.automatically": {
            "type": "string",
            "default": "No",
            "enum": [
              "No",
              "Yes",
              "Ask"
            ],
            "enumDescriptions": [
              "Do not automatically push created branches",
              "Automatically push created branches to remote",
              "Ask to push to remote when creating branches"
            ]
          }
        }
      },
      {
        "id": "wt-new-vscode-window",
        "title": "Open new Vscode window",
        "properties": {
          "vsCodeGitWorktree.move.openNewVscodeWindow": {
            "type": "boolean",
            "default": true,
            "description": "Open new vscode window when you switch or create a worktree"
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "^1.72.0",
    "@typescript-eslint/eslint-plugin": "^5.38.1",
    "@typescript-eslint/parser": "^5.38.1",
    "@vscode/test-electron": "^2.1.5",
    "eslint": "^8.24.0",
    "glob": "^8.0.3",
    "husky": "^8.0.1",
    "mocha": "^10.0.0",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
